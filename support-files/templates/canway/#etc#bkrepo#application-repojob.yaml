spring:
  application:
    name: ${service.name}
    desc: Job Service
server:
  port: __BK_REPO_JOB_PORT__
logging:
  path: ${logging.path-common}/${service.name}
service:
  name: job
job:
  repoRefreshJobProperties:
    enabled: false
