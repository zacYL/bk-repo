spring:
  application:
    name: ${service.prefix}${service.name}${service.suffix}
    desc: pypi Service
server:
  port: __CPACK_PYPI_PORT__
logging:
  path: ${logging.path-common}/${service.name}
service:
  name: pypi
pypi:
  domain: ${cluster.self.url}/pypi
